docker commands to get the tflite model from keras model
docker build -t asl-tflite-test .                 
docker run --rm -it -v $(pwd):/app asl-tflite-test


to run on rasberry
craete virtual env
source ~/asl_env/bin/activate
for running on rasberry pi ensure these versions
Python 3.11.2
NumPy version: 1.26.4
OpenCV version: 4.9.0
tensor flow lite 2.14.0

model used asl_model.tflite on rasberry branch (DO NOT TOUCH IT!!!!)


to run webcam on pi, run serverForWebcam using virtual env on mac
and run webcamClientPi on rasberry Pi on virtual env, no additional stuff needed